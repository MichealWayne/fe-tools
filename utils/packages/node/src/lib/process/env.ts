// @document https://www.npmjs.com/package/minimist, need install
// eslint-disable-next-line @typescript-eslint/no-var-requires
const argv = require('minimist')(process.argv.slice(2));

export default argv;
